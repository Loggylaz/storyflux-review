# --- App
NODE_ENV=development
SERVER_PORT=4000
WEB_ORIGIN=http://localhost:3000
NEXT_PUBLIC_SERVER_URL=http://localhost:4000

# --- Postgres (Docker par défaut)
# NOTE : Prisma lit packages/db/prisma/.env à l'exécution des commandes DB.
DATABASE_URL=postgresql://storyflux:storyflux@localhost:5432/storyflux?schema=public

# --- Redis (réservé pour plus tard)
REDIS_URL=redis://localhost:6379

# --- Sécurité
# Utilisé pour signer les jets de dés côté serveur (HMAC). Mets une valeur aléatoire.
DICE_SECRET=changeme-please

# --- TTS Provider selection: openai | none
TTS_PROVIDER=openai

# --- OpenAI (recommandé)
OPENAI_API_KEY=sk-xxxxxx
OPENAI_TTS_MODEL=gpt-4o-mini-tts
OPENAI_TEXT_MODEL=gpt-4o-mini
OPENAI_VOICE_NARRATOR=marin
OPENAI_VOICE_NPC1=alloy
OPENAI_VOICE_NPC2=verse

# --- Azure (optionnel, si tu testes plus tard)
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=westeurope
AZURE_VOICE_NARRATOR=fr-FR-DeniseNeural
AZURE_VOICE_NPC1=fr-FR-HenriNeural
AZURE_VOICE_NPC2=fr-CA-ThierryNeural

# --- ElevenLabs (optionnel)
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_NARRATOR=VOICE_ID_1
ELEVENLABS_VOICE_NPC1=VOICE_ID_2
ELEVENLABS_VOICE_NPC2=VOICE_ID_3
